/**
 * @file
 */

#pragma once

#include <vector>
#include "video/Shader.h"

namespace $namespace$ {

class $name$ : public video::Shader {
public:
	/**
	 * @brief Load the vertex and fragment shaders and verfies that its attributes and uniforms are used.
	 * @note If an attribute or an uniform isn't active, a message will be printed about that fact - but
	 * the setup process won't fail.
	 */
	bool setup() {
		if (!loadProgram("$filename$")) {
			return false;
		}
		$attributes$
		$uniforms$
		return true;
	}
$setters$
};

typedef std::shared_ptr<$name$> $name$Ptr;

};
